```
═══════════════════════════════════════════════════════════════════════════
                    J.A.R.V.I.S DATABASE ARCHITECTURE
═══════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────┐
│                          FRONTEND (React/HTML)                          │
│                     https://vishai-f6197.web.app                        │
└────────────────────┬────────────────────────────────┬───────────────────┘
                     │                                │
                     │ POST /chat                     │ GET /history
                     │ {"message": "..."}             │
                     ▼                                ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    FLASK BACKEND (Python)                               │
│                https://ai-tutor-jarvis.onrender.com                     │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                     /chat ENDPOINT                               │  │
│  │                                                                  │  │
│  │  1. Receive user message                                        │  │
│  │     └─> save_message("user", query) ────┐                       │  │
│  │                                          │                       │  │
│  │  2. MoE Router: Select model            │                       │  │
│  │     └─> Coding / General / Gemma        │                       │  │
│  │                                          │                       │  │
│  │  3. Tavily Search (if needed)           │                       │  │
│  │     └─> 3000 token limit                │                       │  │
│  │                                          │                       │  │
│  │  4. Call Groq LLM                        │                       │  │
│  │     └─> Get AI response                 │                       │  │
│  │                                          │                       │  │
│  │  5. Save response                        │                       │  │
│  │     └─> save_message("assistant", ans) ─┤                       │  │
│  │                                          │                       │  │
│  │  6. Return JSON                          │                       │  │
│  └──────────────────────────────────────────┼───────────────────────┘  │
│                                             │                           │
│  ┌─────────────────────────────────────────┼───────────────────────┐  │
│  │                  /history ENDPOINT      │                       │  │
│  │                                         │                       │  │
│  │  1. Connect to database                 │                       │  │
│  │  2. SELECT last 20 messages (DESC)      │                       │  │
│  │  3. Convert to JSON dictionaries ◄──────┤                       │  │
│  │  4. Return {"messages": [...]}          │                       │  │
│  └─────────────────────────────────────────┼───────────────────────┘  │
│                                             │                           │
│  ┌─────────────────────────────────────────┼───────────────────────┐  │
│  │              HELPER FUNCTIONS           │                       │  │
│  │                                         │                       │  │
│  │  init_database()                        │                       │  │
│  │  ├─> CREATE TABLE IF NOT EXISTS         │                       │  │
│  │  └─> Run on startup                     │                       │  │
│  │                                         │                       │  │
│  │  save_message(role, content) ◄──────────┘                       │  │
│  │  ├─> INSERT INTO chat_history                                  │  │
│  │  ├─> Auto-timestamp                                            │  │
│  │  └─> Error handling                                            │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└────────────────────┬────────────────────────────────────────────────────┘
                     │
                     │ All queries
                     ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    SQLite DATABASE                                      │
│                  jarvis_chat_history.db                                 │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │  TABLE: chat_history                                            │  │
│  │                                                                  │  │
│  │  ┌────┬───────────┬──────────────────────┬─────────────────────┐│  │
│  │  │ id │   role    │       content        │     timestamp       ││  │
│  │  ├────┼───────────┼──────────────────────┼─────────────────────┤│  │
│  │  │ 1  │ user      │ What is Python?      │ 2026-02-03 12:30:45 ││  │
│  │  │ 2  │ assistant │ Python is a high...  │ 2026-02-03 12:30:46 ││  │
│  │  │ 3  │ user      │ Tell me about Flask  │ 2026-02-03 12:31:20 ││  │
│  │  │ 4  │ assistant │ Flask is a micro...  │ 2026-02-03 12:31:21 ││  │
│  │  │... │ ...       │ ...                  │ ...                 ││  │
│  │  └────┴───────────┴──────────────────────┴─────────────────────┘│  │
│  │                                                                  │  │
│  │  Features:                                                       │  │
│  │  ✓ Auto-incrementing ID                                         │  │
│  │  ✓ Role-based storage (user/assistant)                          │  │
│  │  ✓ Full message content                                         │  │
│  │  ✓ Automatic timestamps                                         │  │
│  │  ✓ Persistent across restarts                                   │  │
│  └─────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════
                              API FLOW EXAMPLE
═══════════════════════════════════════════════════════════════════════════

USER SENDS MESSAGE:
------------------
POST https://ai-tutor-jarvis.onrender.com/chat
{
  "message": "What is Python?",
  "user_id": "user123"
}

BACKEND PROCESSING:
-------------------
1. save_message("user", "What is Python?")  ──┐
                                              │
2. MoE Router: analyze_intent()               │
   └─> Selected: "general" model              │
                                              │
3. Tavily Search: Not time-sensitive          │
   └─> Skip web research                      │
                                              │
4. Call Groq API:                             │
   └─> llama3-70b-8192                        │
   └─> Response: "Python is a high-level..."  │
                                              │
5. save_message("assistant", response)  ──────┤
                                              │
6. Return JSON response                       │
                                              │
DATABASE WRITES: ◄────────────────────────────┘
┌────┬───────────┬──────────────────────────────┬─────────────────────┐
│ 1  │ user      │ What is Python?              │ 2026-02-03 12:30:45 │
│ 2  │ assistant │ Python is a high-level...    │ 2026-02-03 12:30:46 │
└────┴───────────┴──────────────────────────────┴─────────────────────┘


USER REQUESTS HISTORY:
----------------------
GET https://ai-tutor-jarvis.onrender.com/history

BACKEND PROCESSING:
-------------------
1. Connect to jarvis_chat_history.db
2. Execute: SELECT * FROM chat_history ORDER BY id DESC LIMIT 20
3. Convert sqlite3.Row objects to dictionaries
4. Reverse for chronological order
5. Return JSON

RESPONSE:
---------
{
  "success": true,
  "messages": [
    {
      "id": 1,
      "role": "user",
      "content": "What is Python?",
      "timestamp": "2026-02-03 12:30:45"
    },
    {
      "id": 2,
      "role": "assistant",
      "content": "Python is a high-level programming language...",
      "timestamp": "2026-02-03 12:30:46"
    }
  ],
  "count": 2,
  "timestamp": "2026-02-03T12:31:00.123456Z"
}


═══════════════════════════════════════════════════════════════════════════
                           TECHNICAL STACK
═══════════════════════════════════════════════════════════════════════════

Backend:        Python Flask 3.0.0
Database:       SQLite3 (built-in)
ORM:            None (raw SQL for simplicity)
Row Factory:    sqlite3.Row (dict conversion)
CORS:           Flask-CORS 4.0.0
LLM:            Groq API (4 models)
Search:         Tavily API (3000 token limit)
Vision:         Gemini 1.5 Flash
Memory:         ChatHistory (10 exchanges)
Deployment:     Render (auto-deploy from GitHub)


═══════════════════════════════════════════════════════════════════════════
                         KEY CODE SNIPPETS
═══════════════════════════════════════════════════════════════════════════

DATABASE INITIALIZATION:
------------------------
def init_database():
    conn = sqlite3.connect(DB_PATH)
    cursor = conn.cursor()
    cursor.execute("""
        CREATE TABLE IF NOT EXISTS chat_history (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            role TEXT NOT NULL,
            content TEXT NOT NULL,
            timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
        )
    """)
    conn.commit()
    conn.close()

SAVE MESSAGE HELPER:
--------------------
def save_message(role: str, content: str):
    try:
        conn = sqlite3.connect(DB_PATH)
        cursor = conn.cursor()
        cursor.execute(
            "INSERT INTO chat_history (role, content) VALUES (?, ?)",
            (role, content)
        )
        conn.commit()
        conn.close()
        print(f"💾 Saved {role} message to database")
    except Exception as e:
        print(f"⚠️ Database save error: {e}")

GET HISTORY:
------------
conn = sqlite3.connect(DB_PATH)
conn.row_factory = sqlite3.Row  # Return as dictionaries
cursor = conn.cursor()
cursor.execute("""
    SELECT id, role, content, timestamp
    FROM chat_history
    ORDER BY id DESC
    LIMIT 20
""")
rows = cursor.fetchall()
messages = [dict(row) for row in rows]
messages.reverse()  # Chronological order


═══════════════════════════════════════════════════════════════════════════
                              TESTING
═══════════════════════════════════════════════════════════════════════════

TEST SCRIPT (Python):
---------------------
python test_database.py

TEST UI (Browser):
------------------
Open test_history_ui.html
- Beautiful web interface
- Real-time updates
- Send test messages
- View full history

TEST MANUALLY (curl):
---------------------
# Send message
curl -X POST https://ai-tutor-jarvis.onrender.com/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"Hello JARVIS"}'

# Get history
curl https://ai-tutor-jarvis.onrender.com/history


═══════════════════════════════════════════════════════════════════════════
                          ✅ ALL REQUIREMENTS MET
═══════════════════════════════════════════════════════════════════════════

✓ SQLite database with chat_history table
✓ Modular save_message(role, content) helper
✓ /chat endpoint saves messages automatically
✓ /history GET endpoint returns last 20 messages
✓ sqlite3.Row for clean JSON dictionaries
✓ CORS configured for React frontend
✓ Existing Groq/Tavily logic intact
✓ Deployed to Render
✓ Test tools created

═══════════════════════════════════════════════════════════════════════════
```
