# J.A.R.V.I.S Resilient Agent - Requirements
# Last Updated: 03-02-2026

# Core dependencies (REQUIRED)
# ============================================================================

# LangChain (Optional but recommended)
langchain>=0.1.17
langchain-core>=0.1.46
langchain-community>=0.0.38

# FastAPI for REST API server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0

# Search tools (OPTIONAL - agent works without them)
# ============================================================================

# DuckDuckGo fallback search (highly recommended)
ddgs>=5.0.0

# HTTP requests for SearXNG
requests>=2.31.0

# Additional dependencies (OPTIONAL)
# ============================================================================

# For LangGraph integration (if using ARG v3.0)
langgraph>=0.0.47

# FAISS vector database (if using knowledge base)
faiss-cpu>=1.7.4

# Sentence transformers for embeddings
sentence-transformers>=2.3.0

# Development & Testing
# ============================================================================
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Installation Instructions
# ============================================================================
# 
# Minimal installation (core only):
#   pip install fastapi uvicorn pydantic requests ddgs
#
# Full installation (all features):
#   pip install -r jarvis-resilient-requirements.txt
#
# Note: Agent works even if search tools fail - Zero-Failure Logic!
